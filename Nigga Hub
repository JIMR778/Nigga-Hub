local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Negger hub", "Sentinel")

-- Main
local Main = Window:NewTab("Main")
local MainSection = Main:NewSection("Main")


MainSection:NewButton("AimLock", "You get AimLock", function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/JIMR778/Nigga-Hub/refs/heads/main/Aimbot"))()
end)






MainSection:NewButton("ESP", "click to get esp", function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/JIMR778/Nigga-Hub/refs/heads/main/Esp"))()
end)



MainSection:NewButton("Zoom In", "You can zoom in with c", function()
local oldZoom = game.Workspace.CurrentCamera.FieldOfView

local Settings = {
    ZoomTime = 0.2,
    ZoomedAmount = 10
}

local function createZoom(time, amount)
    local Tween_Info = TweenInfo.new(time, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut) 
    local Tween = game:GetService("TweenService"):Create(game.Workspace.CurrentCamera, Tween_Info, {FieldOfView = amount})
    return Tween
end

game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.C then
        createZoom(Settings.ZoomTime, Settings.ZoomedAmount):Play()
    end
end)

game:GetService("UserInputService").InputEnded:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.C then
        createZoom(Settings.ZoomTime, oldZoom):Play()
    end
end)
end)






MainSection:NewButton("FullBright", "You se better when is dark", function()
game.Lighting.Ambient = Color3.fromRGB(178, 178, 178)
        if game:GetService("Lighting").Sky then
            game:GetService("Lighting").Sky:Destroy()
        end
        game:GetService("Lighting").GlobalShadows = false
        game:GetService("Lighting").FogEnd = 14881488
        local da = Instance.new("ColorCorrectionEffect")
        da.Name= "da"
        da.Brightness= 0.3
        da.Contrast= 0
        da.Saturation= 0
        da.Enabled= true
end)

MainSection:NewButton("Monkey Hub", "You get Monkey Hub", function()
loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/richie0866/orca/master/public/latest.lua"))()
end)










-- Project Delta
local Main2 = Window:NewTab("Project Delta")
local Main2Section = Main2:NewSection("Project Delta")


Main2Section:NewButton("Swimhub", "you get swimhub", function()
getgenv().key = "0c88b6d83aaff67d07db731711a1013fc5b0bf28707701c14aac536955eec9d1"
loadstring(game:HttpGet("https://api.swimhub.xyz/loader.lua"))()
end)




Main2Section:NewButton("BBC Hub", "you get BBC Hub", function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/JIMR778/Nigga-Hub/main/BBC%20Hub"))()
end)





Main2Section:NewButton("No Recoil", "You get No Recoil", function()
local ammo = game.ReplicatedStorage.AmmoTypes

for i,v in pairs(ammo:GetChildren()) do
     v:SetAttribute("RecoilStrength", "0")
end
end)






Main2Section:NewButton("Inventory Viewer", "Look to peoples inventory with x", function()
pcall(function()
    getgenv().inventoryviewer = nil
end)

getgenv().inventoryviewer = {}
local Environment = getgenv().inventoryviewer

local inCamera = workspace.CurrentCamera
local inRunService = game:GetService("RunService")
local inUIS = game:GetService("UserInputService")
local inRepStorage = game:GetService("ReplicatedStorage")
local inEntities = game:GetService("Players")
local inLocalPlayer = inEntities.LocalPlayer
local inMouse = inLocalPlayer:GetMouse()

local InventoryUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/RelkzzRebranded/agogogogos/main/inventoryUi.lua"))()

local INVENTORYINSTANCES = {
    MainFrame = InventoryUI.MainFrame,
    VictimText = InventoryUI.MainFrame.VictimName,
    InventoryText = InventoryUI.MainFrame.List
}
InventoryUI.Parent = gethui()

local PlayersFolder = inRepStorage.Players

local Title = [[Brickmane]]
local Desc = [[This is an inventory viewer but simple]]

Environment.Settings = {
    Enabled = false,
    TriggerKey = 'X',
    Radius = 2000
}

inUIS.InputBegan:Connect(function(Input, process)
    if process then return end

    if Input.KeyCode == Enum.KeyCode[Environment.Settings.TriggerKey] then
        Environment.Settings.Enabled = not Environment.Settings.Enabled
    end
end)

local function LoadInventoryViewer()
    inRunService.RenderStepped:Connect(function()
        local dist = math.huge
        local closest_character = nil
    
        if Environment.Settings.Enabled then
            for _,v in next , inEntities:GetChildren() do
                pcall(function()
                    if v ~= inLocalPlayer and
                    v ~= nil and
                    v.Character:FindFirstChild('HumanoidRootPart') and
                    v.Character:FindFirstChild('Humanoid') and
                    v.Character.Humanoid.Health > 0 then
                        local char = v.Character
                        local char_part_pos, onscreen = inCamera:WorldToViewportPoint(char.HumanoidRootPart.Position)
        
                        if onscreen then
                            local magnitude = (Vector2.new(inMouse.X, inMouse.Y) - Vector2.new(char_part_pos.X, char_part_pos.Y)).Magnitude
        
                            if magnitude < dist and magnitude < Environment.Settings.Radius then
                                dist = magnitude
                                closest_character = char
                            end
                        end
                    end
                end)
            end
    
            if closest_character ~= nil and
            closest_character.Parent and
            closest_character:FindFirstChild('HumanoidRootPart') and
            closest_character:FindFirstChild('Humanoid') and
            closest_character.Humanoid.Health > 0 then
                InventoryUI.Enabled = true

                local playerInventory = PlayersFolder[closest_character.Name] and PlayersFolder[closest_character.Name].Inventory
                local playerClothing = PlayersFolder[closest_character.Name] and PlayersFolder[closest_character.Name].Clothing

                local PrimaryLoot = ""
                local WearablesLoot = ""
                if playerInventory and playerClothing then
                    for _, item in pairs(playerInventory:GetChildren()) do
                        PrimaryLoot ..=  item.Name .. "\n"
                    end
                
                    for _, clothes in pairs(playerClothing:GetChildren()) do
                        WearablesLoot ..= clothes.Name .. '\n'
                    end
                end

                if PrimaryLoot ~= "" then
                    PrimaryLoot = PrimaryLoot:sub(1, -2) 
                else
                    PrimaryLoot = "NO WEAPONS"
                end

                if WearablesLoot ~= "" then
                    WearablesLoot = WearablesLoot:sub(1, -2) 
                else
                    WearablesLoot = "NAKED"
                end

                INVENTORYINSTANCES.VictimText.Text = closest_character.Name
                INVENTORYINSTANCES.InventoryText.Text = `PRIMARY:\n{PrimaryLoot}\nWearables:\n{WearablesLoot}`
            else
                InventoryUI.Enabled = false
            end
        end
    end)    
end

LoadInventoryViewer(); print('INVENTORY VIEWER LOADED')
end)






Main2Section:NewButton("Bring people", "Brings people", function()
pcall(function()
    getgenv().hhhhhhh = nil
end)

getgenv().hhhhhhh = {}
local Environment = getgenv().hhhhhhh

local inCamera = workspace.CurrentCamera
local inRunService = game:GetService("RunService")
local inUIS = game:GetService("UserInputService")
local inRepStorage = game:GetService("ReplicatedStorage")
local inEntities = game:GetService("Players")
local inLocalPlayer = inEntities.LocalPlayer
local inMouse = inLocalPlayer:GetMouse()

local PlayersFolder = inRepStorage.Players

local Title = [[Brickmane]]
local Desc = [[Fuck Campers]]

Environment.Settings = {
    Enabled = false,
    TriggerKey = 'G',
    Radius = 2000
}

inUIS.InputBegan:Connect(function(Input, process)
    if process then return end

    if Input.KeyCode == Enum.KeyCode[Environment.Settings.TriggerKey] then
        Environment.Settings.Enabled = not Environment.Settings.Enabled
    end
end)

local function LOADINSHIT()
    inRunService.RenderStepped:Connect(function()
        local dist = 500
        local closest_character = nil
    
        if Environment.Settings.Enabled then
            for _,v in next , inEntities:GetChildren() do
                pcall(function()
                    if v ~= inLocalPlayer and
                    v ~= nil and
                    v.Character:FindFirstChild('HumanoidRootPart') and
                    v.Character:FindFirstChild('Humanoid') and
                    v.Character.Humanoid.Health > 0 then
                        local char = v.Character
                        local char_part_pos, onscreen = inCamera:WorldToViewportPoint(char.HumanoidRootPart.Position)
        
                        if onscreen then
                            local magnitude = (Vector2.new(inMouse.X, inMouse.Y) - Vector2.new(char_part_pos.X, char_part_pos.Y)).Magnitude
        
                            if magnitude < dist and magnitude < Environment.Settings.Radius then
                                dist = magnitude
                                closest_character = char
                            end
                        end
                    end
                end)
            end

            if closest_character ~= nil and
            closest_character.Parent and
            closest_character:FindFirstChild('HumanoidRootPart') and
            closest_character:FindFirstChild('Humanoid') and
            closest_character.Humanoid.Health > 0 then  
                local cameraPosition = inCamera.CFrame.Position
                local targetPosition = cameraPosition + (game.Workspace.CurrentCamera.CFrame.LookVector * 9.5)
                targetPosition = Vector3.new(targetPosition.X, targetPosition.Y - 1.6, targetPosition.Z) 
                closest_character.HumanoidRootPart.CFrame = CFrame.new(targetPosition, cameraPosition)
            end
        end
    end)    
end

LOADINSHIT(); print('TP PLR 2 U LOADED', Title ,Desc)
end)
